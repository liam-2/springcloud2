server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #        server-addr: localhost:8848 #配置nacos地址
        server-addr: localhost:8848 # 换成nginx的1111端口做集群
management:
  endpoints:
    web:
      exposure:
        include: "*"